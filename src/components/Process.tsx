import type { LedgerPointData } from "@/types/ledgerpoint";

export function Process({ data }: { data: LedgerPointData["process"] }) { return <section className="bg-white py-[76px] md:py-[116px]"><div className="container"><div className="mb-12 text-center"><p className="eyebrow">{data.eyebrow}</p><h2 className="my-4 text-[clamp(2.3rem,4vw,4rem)] font-semibold">{data.title} <em>{data.accentTitle}</em></h2></div><div className="grid gap-8 md:grid-cols-3 md:gap-0">{data.steps.map((step, index) => <div key={step.number} className="relative pl-11 md:pl-0 md:pr-[10%]"><strong className="absolute left-0 top-0 font-serif text-lg font-medium text-teal md:static">{step.number}</strong><h3 className="mb-2 mt-0 text-base font-bold text-navy md:mt-5">{step.title}</h3><p className="max-w-[210px] text-xs text-[#647276]">{step.description}</p>{index < data.steps.length - 1 && <span className="absolute bottom-[-20px] left-2 h-5 border-l border-[#dce4e1] md:left-auto md:right-[12%] md:top-3 md:h-0 md:w-[55%] md:border-l-0 md:border-t" />}</div>)}</div></div></section>; }
