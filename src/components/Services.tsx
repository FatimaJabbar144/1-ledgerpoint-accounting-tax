import type { LedgerPointData } from "@/types/ledgerpoint";
import { ServiceIcon } from "./ServiceIcon";

export function Services({ data }: { data: LedgerPointData["services"] }) { return <section className="bg-fog py-[76px] md:py-[116px]" id="services"><div className="container"><div className="mb-8 md:mb-12 md:flex md:items-end md:justify-between"><div><p className="eyebrow">{data.eyebrow}</p><h2 className="my-4 text-[clamp(2.3rem,4vw,4rem)] font-semibold">{data.title} <em>{data.accentTitle}</em></h2></div><p className="max-w-[330px] text-sm text-[#647276]">{data.description}</p></div><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-4">{data.items.map((item) => <article key={item.title} className={`flex min-h-[235px] flex-col border-t-[3px] border-transparent p-6 transition hover:-translate-y-1 hover:border-teal md:min-h-[280px] ${item.featured ? "!bg-navy text-white" : "bg-white"}`}><ServiceIcon icon={item.icon} /><h3 className={`my-4 text-lg font-bold leading-tight ${item.featured ? "text-white" : "text-navy"}`}>{item.title}</h3><p className={`text-sm ${item.featured ? "text-[#bfccce]" : "text-[#647276]"}`}>{item.description}</p><a href="#contact" className="mt-auto text-xs font-bold text-teal">{data.learnMoreLabel} <span>↗</span></a></article>)}</div></div></section>; }
