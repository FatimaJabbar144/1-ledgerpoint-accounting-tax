import Image from "next/image";
import type { LedgerPointData } from "@/types/ledgerpoint";

export function About({ data, trustPoints }: { data: LedgerPointData["about"]; trustPoints: LedgerPointData["trustPoints"] }) { return <section className="bg-white py-[76px] md:py-[116px]" id="about"><div className="container grid items-center gap-11 md:grid-cols-2 md:gap-[90px]"><div className="image-frame h-[390px] md:h-[565px]"><Image src={data.image.src} alt={data.image.alt} fill sizes="(max-width: 768px) 100vw, 50vw" className="relative z-[1] object-cover" /><span className="absolute bottom-6 left-0 z-[2] bg-navy px-4 py-3 text-xs text-white">{data.imageLabel}</span></div><div><p className="eyebrow">{data.eyebrow}</p><h2 className="my-4 text-[clamp(2.3rem,4vw,4rem)] font-semibold">{data.title} <em>{data.accentTitle}</em></h2>{data.paragraphs.map((paragraph) => <p key={paragraph} className="max-w-[500px] text-sm leading-relaxed text-[#647276]">{paragraph}</p>)}<div className="mt-8 border-t border-[#dce4e1]">{trustPoints.map((point) => <div key={point.number} className="flex gap-5 border-b border-[#dce4e1] py-3"><strong className="font-serif text-sm font-medium text-teal">{point.number}</strong><span><b className="block text-xs text-navy">{point.title}</b><small className="text-[10px] text-[#647276]">{point.description}</small></span></div>)}</div></div></div></section>; }
