import Image from "next/image";
import type { LedgerPointData } from "@/types/ledgerpoint";

export function WhyChooseUs({ data }: { data: LedgerPointData["whyUs"] }) { return <section className="bg-cream py-[76px] md:py-[116px]" id="why-us"><div className="container grid items-center gap-11 md:grid-cols-2 md:gap-[90px]"><div><p className="eyebrow">{data.eyebrow}</p><h2 className="my-4 text-[clamp(2.3rem,4vw,4rem)] font-semibold">{data.title} <em>{data.accentTitle}</em></h2><p className="max-w-[475px] text-sm leading-relaxed text-[#647276]">{data.description}</p><div className="my-8 grid grid-cols-2 gap-5">{data.benefits.map((benefit) => <div key={benefit} className="flex items-center gap-3 text-xs font-bold"><span className="text-teal">✓</span>{benefit}</div>)}</div><a href="#contact" className="text-link">{data.linkLabel} <span className="ml-2 text-lg">→</span></a></div><div className="image-frame h-[370px] md:h-[495px]"><Image src={data.image.src} alt={data.image.alt} fill sizes="(max-width: 768px) 100vw, 50vw" className="relative z-[1] object-cover" /><span className="absolute bottom-6 right-5 z-[2] flex items-center gap-3 bg-teal px-5 py-4 text-white"><strong className="font-serif text-3xl">{data.stat.value}</strong><small className="text-[10px] leading-tight">{data.stat.label}</small></span></div></div></section>; }
