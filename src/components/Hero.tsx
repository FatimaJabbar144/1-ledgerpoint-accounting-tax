import Image from "next/image";
import type { LedgerPointData } from "@/types/ledgerpoint";

export function Hero({ data }: { data: LedgerPointData["hero"] }) { return <section className="bg-cream pb-20 pt-14 md:pb-[118px] md:pt-[75px]"><div className="container grid items-center gap-11 md:grid-cols-2 md:gap-[90px]"><div><p className="eyebrow">{data.eyebrow}</p><h1 className="my-5 text-[clamp(3.1rem,15vw,6rem)] font-semibold md:text-[clamp(3.5rem,6vw,6rem)]">{data.title}<br /><em>{data.accentTitle}</em></h1><p className="max-w-[430px] text-base leading-relaxed text-[#647276] md:text-lg">{data.description}</p><div className="mt-7 flex flex-col items-start gap-5 md:mb-10 md:flex-row md:items-center md:gap-7"><a className="button" href="#contact">{data.primaryCta}<span>↗</span></a><a className="text-link" href="#services">{data.secondaryCta} <span className="ml-2 text-lg">→</span></a></div><div className="flex items-center gap-3 text-xs text-[#647276]"><span className="flex pl-2.5">{data.avatars.map((avatar, index) => <b key={avatar} className={`-ml-2.5 grid h-7 w-7 place-items-center rounded-full border-2 border-cream text-[8px] text-white ${index === 0 ? "bg-navy" : index === 1 ? "bg-teal" : "bg-[#879694]"}`}>{avatar}</b>)}</span><span>{data.trustNote}<br /><strong className="text-navy">{data.trustYears}</strong></span></div></div><div className="image-frame order-first h-[370px] md:order-none md:h-[540px]"><Image src={data.image.src} alt={data.image.alt} fill priority sizes="(max-width: 768px) 100vw, 50vw" className="relative z-[1] object-cover" /></div></div></section>; }
